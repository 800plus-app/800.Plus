# תיקיית השיווק · 800+

**הכלל היחיד כאן: קובץ מקור חי לצד התוצר שנוצר ממנו.** ה-`ad.html` נמצא בתיקיית הסרטונים ולא בתיקיית התסריטים, כי מי שרוצה לשנות את הסרטון צריך את שניהם באותו מקום. אותו דבר ב-`סטוריז.html`.

---

## 📹 `סרטונים/`

| קובץ | מה זה |
|---|---|
| `800plus-ad.mp4` | הפרסומת · 1080×1920 · 26 שניות · מוכן לפרסום |
| `ad.html` | **המקור** שממנו הסרטון מרונדר. כל שינוי טקסט מתחיל כאן |
| `render.ps1` | מרנדר את `ad.html` לסרטון בפקודה אחת |
| `סרטון-הדרכה-בודקים.mp4` | הדרכה לבודקי הגרסה המוקדמת |

**לשנות טקסט בסרטון:**
1. ערוך את `ad.html`
2. הרץ `.\render.ps1` מתוך `סרטונים/`

הסקריפט **עוצר לפני הרינדור** אם המספר שמופיע בביט החמישי אינו תואם למאגר בפועל. זה לא עיטור — הגרסה הקודמת של הסרטון הכריזה על 5,413 מילים אחרי שהמאגר כבר הגיע ל-5,662.

## 🖼 `תמונות/`

| קובץ | מה זה |
|---|---|
| `סטוריז.html` | שלושה סטוריז אנכיים · מקור לצילום |
| `סטורי-פיילוט.png` | הסטורי שכבר צולם |

## 🎨 `לוגו-ואייקונים/`

עותקי המותג — לשימוש בפרסום, בחנויות אפליקציות וברשתות.

> ⚠️ **אלה עותקים בלבד.** האייקונים החיים של האפליקציה יושבים ב**שורש הפרויקט**, ו-`manifest.webmanifest`, `index.html` ו-`sw.js` מפנים לשם. **אל תעביר את המקוריים לכאן** — זה מוריד לאפליקציה המותקנת את האייקון. זה קרה, ותוקן ב-3.8.2026.

## ✍️ `תסריטים/`

`בריף-סרטון.md` · `תסריט-סרטון-הסבר.md` · `תסריטים-לסרטוני-AI.md` · `סרטון-הדרכה-בודקים.md`

## 📊 `תוכניות/`

`תכנית-שיווק.md` — הערוצים, המסרים והקהל.

## ✉️ `מיילים/`

| קובץ | מה זה |
|---|---|
| `מיילים-תזכורת.md` | הנוסח וכללי השליחה |
| `תבניות-מייל.md` | ההסבר על התבניות |
| `תבניות/` | ארבעת ה-HTML של Supabase Auth + `נושאים.txt` |

**מייל התזכורת עצמו אינו כאן** — הוא חי בקוד, ב-`scripts/send_nudges.py`, לצד ההיגיון שמחליט למי לשלוח (`scripts/pick_nudges.py`). הכלל שקובע: מתחת ל-5 מילים לחיזוק — לא נשלח מייל.

## 🎨 `עיצוב-אתר/`

ראה את ה-`README.md` שבתוכה.

---

## לפני שמפרסמים · צ'קליסט

1. **המספרים בסרטון תואמים למאגר?** `render.ps1` בודק את זה ועוצר. אל תעקוף.
2. **שם כפתור שמוזכר בטקסט קיים עדיין באפליקציה?** `grep` אותו ב-`index.html`. השם `החלף שפה` כבר השתנה ל-`🏠 חזרה` פעם אחת.
3. **הכתובת נכונה?** `800-plus.com`
4. **הניסוח עבר `/HEB`?** כל טקסט שיוצא החוצה, גם מילה אחת.
