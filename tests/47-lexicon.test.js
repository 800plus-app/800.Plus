'use strict';
/* "יושבות" · מונח שנדחה, ואסור שיחזור.
 *
 * הרקע
 * -----
 * הלקסיקון של 800+ נקבע על ידי חגי (ראה ~/.claude/skills/HEB). למילים שהמשתמש שולט בהן · 
 * "בשליטה"; למילים שנופלות · "מילים לחיזוק". "יושבות"/"עדיין לא יושבות" נדחה מפורשות:
 * מונח נרדף מחייב את הקורא לתרגם, ופוגע באחידות השפה בממשק.
 *
 * שני מופעים נמצאו · בדף ההדפסה של מילות החיזוק, ובגוף התראת "יומיים בלי תרגול" (ששם נלווה
 * לו גם "עשר דקות", טענה שאי אפשר לאמת מול הקוד). שניהם תוארו מילים חלשות, ולכן המונח הנכון
 * הוא "מילים לחיזוק".
 *
 * הבדיקה שומרת על זה קדימה: אם מישהו יכתוב "יושבות" שוב, כאן זה נופל.
 */

const { test, describe } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');
const path = require('path');
const { ROOT } = require('./_harness/sandbox.js');

const files = ['app.js', 'index.html'];

describe('לקסיקון · "יושבות" אינו מופיע בממשק', () => {
  for (const f of files) {
    test(`${f} אינו מכיל "יושבות"`, () => {
      const src = fs.readFileSync(path.join(ROOT, f), 'utf8');
      const hit = src.includes('יושבות');
      assert.ok(!hit,
        `"${f}" מכיל את המונח הנדחה "יושבות". השתמש ב-"מילים לחיזוק" או "בשליטה" לפי ההקשר.`);
    });
  }
});
