'use strict';
/* אין מקף ארוך (—) בנוסח עברי שמשתמש רואה.
 *
 * למה זה שער ולא הערה
 * -------------------
 * חגי: *"אסור מקפים ארוכים, זה סממן מזהה לAI"*. ב-14.8.2026 הוסרו 1,496
 * מופעים מהערות הקוד ומכותרות הבדיקות, ועוד עשרות מטקסט שמשתמש רואה.
 * בלי שער, המופע הבא ייכנס בשקט בעריכה הבאה — בדיוק כמו שקרה עם מספרי
 * המילים במסך ההרשמה, שנכתבו נכון פעם אחת ואז התיישנו.
 *
 * מה נבדק
 * -------
 * `—` שיש לידו אות עברית. זה מכסה נוסח בכל צורה — מחרוזת רגילה, template
 * מרובה שורות, ותוכן HTML — בלי להיתלות בפרסור מדויק של קוד, שכבר נשבר
 * פעם אחת על regex שמכיל גרשיים.
 *
 * מה **לא** נבדק, במכוון
 * ---------------------
 * מקף ארוך בתוך מחלקת תווים ב-regex (`app.js` שורות 318, 330, 628). שם הוא
 * מסיר מקפים מקלט המשתמש, ולידו אין אות עברית — ולכן הכלל שלמעלה עובר עליו
 * מעצמו, בלי רשימת היתרים שתתיישן.
 *
 * וגם לא `data.js` / `data-en.js`: שם נשארו שלושה מקפים **מבניים**, ושתי
 * בדיקות אחרות נשענות עליהם (`02-bank` על `Latin — עברית` כסימן לצירוף
 * מתורגם, ו-`66-gloss-fragments` על המקף כמפריד שאינו `;`). ניסיון להחליף
 * אותם ב-14.8 הפיל את שתיהן, וזה בדיוק מה שהן נועדו לתפוס.
 */

const { test } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');
const path = require('path');

const שורש = path.join(__dirname, '..');
const קבצים = ['index.html', 'app.js', 'store.js', 'sw.js'];

/** מחזיר כל מופע של — שיש אות עברית במרחק 25 תווים ממנו. */
function מופעים(טקסט) {
  const out = [];
  const re = /—/g;
  let m;
  while ((m = re.exec(טקסט))) {
    const s = Math.max(0, m.index - 25), e = Math.min(טקסט.length, m.index + 25);
    const סביבה = טקסט.slice(s, e);
    if (/[א-ת]/.test(סביבה)) {
      out.push(`שורה ${טקסט.slice(0, m.index).split('\n').length}: …${סביבה.replace(/\n/g, ' ')}…`);
    }
  }
  return out;
}

for (const f of קבצים) {
  test(`אין מקף ארוך בנוסח עברי · ${f}`, () => {
    const p = path.join(שורש, f);
    if (!fs.existsSync(p)) return;
    const hits = מופעים(fs.readFileSync(p, 'utf8'));
    assert.deepStrictEqual(hits, [],
      `נמצא מקף ארוך ליד טקסט עברי ב-${f}. /HEB §3א: להחליף ב-· , או בנקודה.\n  ` +
      hits.join('\n  '));
  });
}

/* ⚠ שיניים. שער שעובר בלי שהוכח שהוא מסוגל ליפול אינו עדות. */
test('לשער יש שיניים · מקף שתול ליד עברית נתפס', () => {
  assert.strictEqual(מופעים('const t = "היחידה נסגרה — כל הכבוד";').length, 1);
});

test('לשער אין שיניים על regex · מקף בלי עברית לידו עובר', () => {
  assert.deepStrictEqual(מופעים(".replace(/[-–—/|]/g,' ')   // separator, not noise"), []);
});
