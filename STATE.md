# STATE · ריצת המשימות א'–ג' (11.8.2026)

קובץ המשך: סשן חדש קורא אותו וממשיך מהנקודה האחרונה בלי הקשר נוסף.

**מוסכמות הריצה הזו:** בלי npm/build · `node tests/run.js` אחרי כל שינוי, 0 כשלים ·
`data.js` · `data-en.js` · `supabase.min.js` נעולים (לא לקרוא, לא לערוך) ·
⛔ **בלי `git push`** — הדחיפה ידנית של חגי בלבד · תת-סוכנים בקריאה בלבד.

---

## משימה א' · ספירה לאחור למבחן — 🟢 הושלמה

**הממצא המרכזי:** הפיצ'ר כבר היה בנוי כמעט במלואו. לא נבנה מסך חדש ולא שוכפלה לוגיקה.

מה שכבר היה קיים (ואומת, לא הונח):

| דרישה | מצב לפני | היכן |
|---|---|---|
| שדה תאריך במסך הגדרות | ✅ קיים | `index.html:1504` · `#accExam` בתוך מסך `account` |
| אחסון + סנכרון ענן | ✅ קיים | `EXAM_KEY='hw_examDate'` · נוסע ב-extras blob (`collectExtras`/`applyExtras`) → `flushRemoteSync` |
| תאריך אחד גלובלי, לא לפי שפה | ✅ קיים | מתועד ב-`app.js:4614` |
| חישוב ימי לוח (חצות/שעון קיץ) | ✅ קיים | `examDays()` · חצות מקומי בשני הקצוות + `Math.round` |
| תצוגה במסך הבית | ✅ קיים | `renderExamPill()` · `#examPill` |
| כללי קצה: היום / מחר / יומיים | ✅ קיים | ניסוח עברי תקין למספר |
| `min` = היום | ✅ קיים | `renderAccExam` |
| aria-label לשדה | ✅ קיים | `index.html:1507` |
| בדיקות לחישוב הימים | ✅ קיימות | `tests/11-time.test.js` — כולל שעון קיץ ושעות היום |

הפערים שנסגרו בריצה הזו:

1. **`max` = שנתיים קדימה** (`renderAccExam`) — לא היה. תאריך רחוק יותר הוא טעות הקלדה בשנה.
2. **ציוני דרך** — `examTip(days)` חדש: 30+ · 14–30 · 7–13 · 1–6 · יום המבחן (בלי טיפ).
   יורד לשורה שנייה ושקטה (`.pill-tip`) כדי לא להוסיף חוליה רביעית לשורת המספרים.
   הניסוח נשען על הלקסיקון הקיים ("מילים שטרם תרגלת" · "לחיזוק" · "בשליטה").
3. **תאריך שעבר** — קודם השורה נעלמה לגמרי וזה נקרא כאילו האפליקציה שכחה את המועד.
   עכשיו: "מועד המבחן שהגדרת עבר · לחץ לעדכון המועד הבא", ולחיצה פותחת את ההגדרות.
4. **`role="status"`** על `#examPill` — קורא מסך מקריא את השורה כשהיא מופיעה או משתנה.

**בדיקות:** 929 → 933. ארבע חדשות ב-`tests/11-time.test.js`:
גבולות ציוני הדרך (31/30, 14/13, 7/6) · תאריך שעבר מציע עדכון ולחיץ · אין תאריך או
תאריך רחוק מדי ⟵ אין שורה · הפיל נושא את ציון הדרך ומשמיט אותו ביום המבחן.
**הוכחת שיניים:** שבירה מכוונת של ענף "התאריך עבר" ושל גבול ה-7 ימים הפילה בדיוק את
הבדיקה המתאימה, ואז שוחזר. 0 כשלים.

**הערה:** תאריך רחוק מ-400 יום עדיין לא מוצג במסך הבית (שמירה על ההתנהגות הקיימת),
אבל **כן** מוצג בהגדרות. ה-`max` החדש מתיר שנתיים, ולכן יש טווח 401–730 יום שבו
המשתמש רואה את הספירה רק בהגדרות. זה מכוון: ספירה בת יותר משנה היא רעש במסך הבית.

---

## משימה ב' · ביקורת "מילים לחיזוק" — ⬜ טרם התחילה

## משימה ג' · משפטי הקשר — ⬜ טרם התחילה

**ממצא מקדים שכבר אומת (לפני שלב 0 הפורמלי):** קיימים כבר **2,015 משפטי עברית**
ב-`sentences/batches/out-s-u{1..10}-00{1..4}.tsv` — כיסוי מלא של 2,015 מילות היחידות,
עם תדריך כתיבה ב-`sentences/BRIEF.md` שכולל את אותם כללי תוכן שקובץ המשימות מבקש.
פירוט והכרעה — בסעיף משימה ג' כשיגיע תורו.
