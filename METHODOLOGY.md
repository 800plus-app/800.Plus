# מתודולוגיה — איך נבנה מאגר המילים

מסמך זה מתעד את העבודה שנעשתה על מאגר המילים של האפליקציה: מאיפה הגיעו המועמדים,
מה נעשה בהם, ומה קיים בו כתוצאה מעבודה מקורית. הוא נכתב כדי שאפשר יהיה לבדוק כל
טענה כאן מול חומר שמור, ולא כדי להציג תמונה נוחה.

**עדכון אחרון:** 30 ביולי 2026.

---

## 1. מאיפה הגיעו המועמדים

רשימת המילים הראשונית לוקטה מסטים ציבוריים של כרטיסיות לפסיכומטרי, בעיקר מ-Quizlet.
**אנחנו לא מסתירים את זה ולא מנסים.** שתי סיבות:

1. זה מה שקרה, ומסמך שמתאר משהו אחר חסר ערך.
2. **הטיעון החוקי החזק כאן אינו הכחשה אלא טרנספורמציה.** סעיף 4(ב) לחוק זכות
   יוצרים התשס"ח-2007 מגן על לקט לפי "המקוריות בבחירה ובסידור". להוכיח מקוריות
   בבחירה ובסידור אפשר רק אם מתעדים מה נעשה בפועל. הכחשה מוותרת על הטיעון הזה.

חשוב להבחין בין שני דברים שנוטים להתבלבל:

- **זוג (מילה, תרגום) הוא עובדה.** סעיף 5 לאותו חוק קובע שזכות יוצרים אינה חלה על
  "עובדה או נתון". `indifferent = אדיש` אינו יצירה של אף אחד.
- **הגדרה בפרוזה היא טקסט, וטקסט מוגן.** זו החשיפה האמיתית, והיא הסיבה לעבודת
  השכתוב המתועדת בסעיף 4 למטה.

רשימות המילים לפסיכומטרי בישראל מגיעות במקורן מחומר פסיכומטרי מפורסם, ולא מיוצריהם
של סטי הכרטיסיות. גם לבוני הסטים אין טענת לקט חזקה על הבחירה עצמה.

**מדיניות מכאן והלאה:** אין איסוף נוסף מ-Quizlet, לא ידני ולא אוטומטי. תוספות
עתידיות יבואו ממקורות שהרישיון שלהם מתיר את השימוש (Wikidata Lexemes ב-CC0,
Hebrew WordNet), או מניסוח עצמאי מול מילונים.

---

## 2. מבנה המאגר כפי שהוא היום

| | עברית | אנגלית |
|---|---|---|
| ערכים | 1,719 | 3,900 |
| יחידות | 10 (+ יחידה אישית) | 10 |
| כפילויות | 0 | 0 |
| פירושים ריקים | 0 | 0 |

הקבצים: `data.js`, `data-en.js`.

---

## 3. ניקוי כפילויות ונרמול — עבודה שלא הייתה במקור

המועמדים הגיעו מכמה סטים שונים שחפפו זה לזה. העבודה שנעשתה:

1. **מפתח נרמול משלנו.** נכתבו שתי פונקציות נרמול (`norm` לעברית, `normEn` לאנגלית)
   שמנרמלות ל-NFKC, מסירות ניקוד וסימני פיסוק, מקפלות אותיות סופיות, ומסירות תווית
   יידוע פותחת באנגלית. שני ערכים שנורמלו לאותו מפתח נחשבים לאותה מילה.
2. **22 כפילויות בעברית שהיו סמויות** אפילו למפתח הזה — כתיב חסר מול כתיב מלא —
   זוהו ואוחדו ידנית, כולל מיזוג של המשמעויות משתי הרשומות לרשומה אחת.
3. **תקלת תווים שנמצאה ותוקנה:** המילה `אוּד` הייתה כתובה בצורת תצוגה
   (U+FB35 במקום ו+שורוק), כך שהקלדת "אוד" סומנה כשגויה. התיקון הוסיף נרמול NFKC
   לכל המאגר: 830 שינויי תווים, שכולם נבדקו כשקילות קנוניות מלאות — אפס שינוי הרסני.
4. **מבנה היחידות** נקבע אצלנו, וכן ההחלטה על ערך אחד לכל מילה עם משמעויות מאוחדות
   במקום רשומות נפרדות.

תוצאה: אפס כפילויות בשתי השפות, כפי שנמדד בסעיף 2.

---

## 4. ביקורת פירושים ותרגומים — 20 סוכני ביקורת ואימות כפול

**סבב א' — איתור.** עשרים ביקורות עצמאיות, אחת לכל יחידה בכל שפה, אימתו כל שורה מול
מילונים (אבן-שושן, רב-מילים, מילוג, האקדמיה ללשון; Oxford, Cambridge,
Merriam-Webster, Collins, Wiktionary). **1,617 ממצאים**, מסווגים:

| סוג | מספר |
|---|---|
| זוגות שהפירוש שלהם קרוב מדי זה לזה | 851 |
| פירוש לא שמיש כפריט מבחן (מעגלי, קטוע, כללי מדי) | 332 |
| המשמעות המשנית מופיעה לפני הראשית | 169 |
| שגיאות בפירוש | 126 |
| סומן כלא-ודאי, לא נגעו בו | 139 |

**סבב ב' — הפרכה.** 126 טענות השגיאה הועברו לשישה מאמתים עצמאיים, שכל אחד מהם נדרש
**לנסות להפריך** כל טענה מול שני מקורות שקרא בפועל, ולהכריע CONFIRMED / REJECTED /
UNSURE. כלל ההכרעה: ספק כלשהו ⇒ לא נוגעים.

**התוצאה: 62 אושרו, 62 נדחו, 2 נותרו בספק. רק 62 הוחלו.**

62 הדחיות הן חלק מהעבודה ולא כישלון שלה: הן מנעו שיבוש של ערכים תקינים. דוגמאות
לטענות שנדחו, כשהמקור שהמאתר עצמו ציטט סתר אותן: `soon`→מוקדם, `before`→מול,
`front`→להתעמת, `splendid`→מהולל, `regardless`→למרות, `rudimentary`→יסודי.

**הממצא המשמעותי ביותר:** תשעה ערכים איחדו שתי מילות מילון שנבדלות רק בניקוד —
`אֵבוּס`/`אָבוּס`, `נְאָקָה`/`נָאקָה`, `נֵצֶר`/`נָצַר`, `שִׁוֵּעַ`/`שׁוֹעַ`,
`מַחַט`/`מָחַט`. ב-`מַחַט` נעדרה המשמעות הראשית לחלוטין. כולם נוסחו מחדש.

חומר שמור: `דוחות/ביקורת-מבחנים/ממצאים-*.md` (20 דוחות),
`דוחות/ביקורת-מבחנים/הוכרע-*.md` (6 דוחות אימות),
`דוחות/ביקורת-פירושים-ותרגומים.html` (סבב מוקדם, 251 תיקונים).

---

## 5. דירוג שכיחות וקושי — נגזר אצלנו

3,175 מהמילים באנגלית קיבלו דירוג שכיחות מקורפוס כתוביות
([hermitdave/FrequencyWords](https://github.com/hermitdave/FrequencyWords), רשימות
תחת CC BY-SA 3.0 — שימוש כאן הוא כמדד חיצוני, לא כמקור שממנו נגזר המאגר).
הדירוג נמצא ב-`enrank.js`, והוא הבסיס לחלוקה לרמות CEFR במבחן הרמה.

הערה על שקיפות מתודית: הניסיון הראשון לחשב "דרגת קושי" נשלט כל כך על ידי מספר
היחידה שהתוצאה רק שיחזרה אותו. הוא נזרק והוחלף במטריצה דו-ממדית של שכיחות מול יחידה.
`דוחות/דירוג-שכיחות-וקושי-אנגלית.csv` ו-`.html`.

---

## 6. מבחן הרמה — בנק פריטים שנוצר אצלנו

`leveltest.js` — 60 פריטים, 10 לכל רמת CEFR, **נוצר בסקריפט משלנו**
(`scratchpad/genlevel.js`) מתוך המאגר, ולא נלקח מאף מקום. הכללים שהסקריפט אוכף:

- מסיחים מאותה רמת שכיחות ומאותו סוג מילה כמו התשובה, כדי שלא ניתן לפתור בפסילה
- ~180 מילים רבות-משמעות מוחרגות, כדי שמי שיודע את המשמעות הרגילה לא ייענש
- תשובה של פריט אחד אינה מסיח של פריט אחר

המנוע מסתגל: בלוקים של 6, 5 נכונות לעלייה. נמדד ב-300 סימולציות לכל רמה:
87–98% שחזור מדויק.

---

## 7. מנוע המבחנים — נבדק על 10,225 פריטים

מבחני היחידה אינם כתובים ידנית: הם נבנים מהנתונים של היחידה בזמן ריצה, בשלושה
פורמטים (זיהוי, שליפה, כתיבה עצמאית). נכתבה בדיקה אוטומטית שבנתה **525 מבחנים על כל
יחידה בשתי השפות, 10,225 פריטים**, ואימתה שכל פריט עומד בשבעה כללים. הבדיקה חשפה
שלושה פגמים אמיתיים שתוקנו:

1. מסיח שהוא התשובה של פריט אחר — מסגיר פתרון שאלה מוקדם
2. שני מסיחים שנבדלים רק בניקוד — הופך שאלה של ארבע לשאלה של שלוש
3. פריטים שאי אפשר לבחון (תבניות משפט, פירוש שהוא תחילית דקדוקית)

תוצאה סופית: אפס הפרות ב-10,225 פריטים.

---

## 8. סיכום העבודה המקורית

| מה | ראיה |
|---|---|
| מפתח נרמול ומיזוג משלנו | `app.js` — `norm`, `normEn`, `buildBank` |
| 22 כפילויות סמויות אוחדו ידנית | סעיף 3 |
| 830 נרמולי תווים, כולם מאומתים כשקילות | סעיף 3 |
| 1,617 ממצאי ביקורת מ-20 בדיקות עצמאיות | `דוחות/ביקורת-מבחנים/` |
| 126 טענות שגיאה עברו אימות כפול; 62 הוחלו, 62 נדחו | `דוחות/ביקורת-מבחנים/הוכרע-*.md` |
| 162 פירושים נוסחו מחדש מאפס מול שני מילונים | `דוחות/שכתוב-פירושים/`, סעיף 9 |
| 251 תיקוני פירוש בסבב מוקדם | `דוחות/ביקורת-פירושים-ותרגומים.html` |
| דירוג שכיחות ל-3,175 מילים + מטריצת קושי | `enrank.js`, `דוחות/דירוג-*` |
| בנק מבחן רמה בן 60 פריטים שנוצר אצלנו | `leveltest.js`, `scratchpad/genlevel.js` |
| מנוע מבחנים שנבדק על 10,225 פריטים | סעיף 7 |
| מבנה יחידות, החלטת ערך-אחד-למילה, כיווני תרגול | `data.js`, `app.js` |

---

## 9. שכתוב פירושים מניסוח עצמאי

### סבב א'

60 הפירושים המנוסחים ביותר במאגר (הארוכים ביותר, אלה שבהם יש ביטוי ולא רק עובדה)
נוסחו מחדש מאפס. הכלל שהוגדר לכותבים: לחפש את המילה **בשני מילונים** ולכתוב הגדרה
חדשה מתוך מה שנקרא — לא לשפר את הפירוש הקיים ולא להסתמך עליו; ובכל ספק, `SKIP`.

הכתיבה נבדקה בסקריפט (`scratchpad/verifyrewrite.js`) שאוכף שבעה כללים, ובהם:

- המילה בדוח חייבת להיות המילה שבשורה בבנק
- "הפירוש הנוכחי" בדוח חייב להיות מה שבבנק, אחרת הדוח מיושן ונדחה
- **חפיפה של יותר ממחצית המילים למקור ⇒ דחייה**, כי שכתוב קוסמטי מפספס את המטרה
- אסור שהפירוש יכיל את המילה הנשאלת (מעגליות)
- עד 12 מילים לכל משמעות, בלי דוגמאות בסוגריים ובלי ניקוד

**תוצאה בסבב א': 50 הוחלו, 8 נדחו, 2 סומנו `SKIP` לצורך הכרעה.** אורך ממוצע ירד מ-15.6
מילים ל-11.3; חפיפה ממוצעת למקור בפירושים שהוחלו — 27%.

שמונה הדחיות נדחו על חפיפה גבוהה, וכולן מאותו סוג: הפירוש המקורי היה בעיקרו רשימת
נרדפות (`גֶּרֶם` — "עצם"), כלומר עובדה שאין בה ביטוי לשכתב. שם אין רווח והפירוש נשאר.

**חיפוש הניסוח העצמאי חשף שגיאות תוכן שסבב הביקורת לא מצא**, כי ביקורת שואלת "האם
הפירוש נכון" ושכתוב מכריח לשאול "מה המילה אומרת". שמונה מהן:

| מילה | מה נמצא |
|---|---|
| `בַּד` | המשמעות הראשית והנפוצה — אריג — נעדרה לגמרי; נשארו רק שתי נדירות |
| `כּוֹתֶרֶת` | המשמעות המרכזית (שם בראש טקסט) נעדרה, והפירוש היה מעגלי |
| `שׁוֹחֵר` | נשא את המשמעות "נאמן, ידיד", שנגזרה בטעות מהצירוף "שוחר שלום" |
| `אָצַר` | "העלים, החביא" אינו מופיע באף מקור; המשמעות היא שמירה וצבירה |
| `מֹרֶך לֵב` | הוגדר כ"בעתה, אימה" — פחד עז — במקום פחדנות וחולשת רוח |
| `הלך שבי` | עורבב עם הניב "הלך שולל"; אין בו מרכיב של הטעיה |
| `שָׁחוּק` | "שדוף" שגוי — שדוף הוא כחוש, לא נדוש |
| `גֵּב` | "בור, גוב אריות" שייך למילה אחרת (גֹּב) משורש נפרד |

וכן תקלת נתונים: ב-`אזלת יד` אותה הגדרה הופיעה **פעמיים באותה שורה**. כל המאגר נסרק
לדפוס הזה בשתי השפות — זה המקרה היחיד.

### סבב ב'

60 פירושים נוספים — הבאים בתור לפי אותו דירוג, 64–94 תווים — עברו את אותו תהליך
ואת אותו סקריפט. **54 הוחלו, 3 נדחו על חפיפה, 3 סומנו `SKIP`.** האיכות עלתה:
חפיפה ממוצעת 18% (מול 27% בסבב א'), אורך 12.8 → 9.5 מילים.

**סך שני הסבבים: 162 פירושים נוסחו מחדש מאפס מול שני מילונים.**

שגיאות תוכן שנחשפו בסבב ב', מעל ומעבר לשכתוב:

| מילה | מה נמצא |
|---|---|
| `מָעַל` | הוצגה רק גזלת כספים; המשמעות הראשית והרחבה היא הפרת אמון |
| `מוּשְׂכָּל רִאשׁוֹן` | "אינה ניתנת להוכחה" — בפועל אינה *זקוקה* להוכחה |
| `קוֹטֵל קָנִים` | "חסר השכלה" — במקורות חסר מיומנות ובקיאות |
| `הִלְאִים` | "לבעלות הכלל" — היעד הוא המדינה |
| `חֶשְׁבּוֹנִיָּה` | הוגבל לחיבור וחיסור; המילונים: ארבע פעולות |
| `סֶכֶר` | תואר כ"ניתן להרימו ולהורידו" — זה שער-סכר ולא סכר |
| `מַרְשֶׁה` | נבדק ואושר **כפי שהיה**: מַרְשֶׁה נותן ההרשאה, מוּרְשֶׁה מקבלה |

וכן **שבעה פירושים מעגליים** (דָּרְבָּן, הוֹלֵל, אַשְׁפָּרָה, אֲלוּמָּה, רַאוּוְתָן,
דַּעְתָן, דִּמְעוֹת תַּנִּין) — הכשל הנפוץ ביותר שהשכתוב חושף, ופריט מבחן חסר תועלת.

**תיקון ניקוד:** `שָׂפַת` נכתב בשי"ן שמאלית; שורש הפועל הוא ש־ב־ת/ש־פ־ת בשי"ן ימנית
(השיבוש כנראה מ`שָׂפָה`). תוקן ל-`שָׁפַת`. כל 47 הערכים שיש בהם שי"ן שמאלית מנוקדת
נבדקו — זה המקרה היחיד השגוי. הערה חשובה על חומרה: פונקציית הנרמול מסירה ניקוד
לפני ההשוואה, ולכן **התקלה לא פגעה בבדיקת התשובות** אלא בתצוגה ובהקראה בלבד. זו
אינה תקלה מסוג `אוּד`.

**ארבעה פריטים סומנו לפיצול ולא לשכתוב** — פריט שמאחד שם עצם ופועל תחת ניקוד אחד:
`כֶּבֶשׁ`/כָּבַשׁ, `גִּדֵם`/גִּדֵעַ, `רֶשֶׁף`/רָשַׁף, `חָפַן`/חֹפֶן. ארבעה מתוך 120
שנבדקו הוא 3%, ולכן הדפוס מחייב סריקה שיטתית ולא טיפול נקודתי. זהו אותו דפוס של
תשעת ההומוגרפים מסעיף 4.

### סבב ג'

60 פירושים, 55–58 תווים. **58 הוחלו, 0 נדחו, 2 סומנו `SKIP`.** חפיפה ממוצעת 15% —
הנמוכה בשלושת הסבבים.

**סך שלושת הסבבים: 162 פירושים נוסחו מחדש מאפס מול שני מילונים.**
נשארו 20 פירושים מעל 60 תווים.

שגיאות תוכן בסבב ג':

| מילה | מה נמצא |
|---|---|
| `כִּמְטַחֲוֵי קֶשֶׁת` | "קרוב, בהישג יד" — במקור ובמילונים זה **מרחק** בטווח חץ |
| `פורס מז'ור` | חסרה הליבה: עילה משפטית לביטול חוזה או פטור מהתחייבות |
| `דְפוּס` | חסרה משמעות ההדפסה — המרכזית בשני המילונים |
| `נֶקְטָר` | חסר צוף הפרחים, השימוש הנפוץ ביותר |
| `שִׁיפּוּי` | "ליטוש בשופין" אינו קיים באף מילון; שם הפעולה לליטוש הוא שִׁיּוּף |
| `עֹול` | תואר רק כלי רתמה; המשמעות הראשית היא נטל מעיק |
| `לִיטֵּשׁ` | סדר המשמעויות הפוך למילונים |
| `אָמָה` | "כ-45 ס"מ" — **דיוק מדומה**; המקורות נותנים 40–60 ס"מ |
| `טרוט עיניים` | נבדק בחשד לשגיאה ואושר **כפי שהיה** |

ועוד **תשעה פירושים מעגליים** (חָרִישׁ, עִישֵּׂב, חַתְרָנוּת, תִּכְתוֹבֶת, קַצְרָנוּת,
שְׁנִיּוֹנִי, מַרְצֵעַ, הִזְדַּכְּכוּת, מַהְדִּיר, חָתָה).

**מעגליות הופיעה בכל אחת מ-12 המנות בלי יוצא דופן** — 20 מקרים ב-180 פירושים,
כלומר ~11%. ההערכה של 332 פירושים לא שמישים מדוח הביקורת (סעיף 4) מתאשרת ואף
נראית שמרנית.

### תקלת פירוש כפול — ותיקון של קביעה שגויה שלי

סוכן מצא ב-`אָזְלַת יָד` את אותה הגדרה כתובה פעמיים בשורה אחת. סרקתי את שתי השפות
לחלקים חוזרים בין מפרידי פסיק ונקודה-פסיק והצהרתי שזה המקרה היחיד. **הקביעה הזאת
הייתה שגויה.** סוכן אחר מצא אחר כך את אותו דפוס ב-`בִּמְשׂוּרָה`, שהסריקה שלי
פספסה כי החזרה שם לא התיישרה עם המפרידים.

הסריקה הוחלפה בבדיקה חזקה יותר — **כל רצף חוזר של שלוש מילים ומעלה בכל מקום
בפירוש** — ונמצאו שלושה מקרים נוספים: `בּוּר` ("שדה לא מעובד" פעמיים), `הוּעַם`
("הפך לקהה, טושטש" פעמיים) ו-`שִׁסֵּעַ` (דוגמה בסוגריים וניקוד שגוי). כולם תוקנו,
ואחרי התיקון הבדיקה מחזירה **אפס חזרות ב-1,719 הערכים**.

הלקח המתודי, וזו הסיבה שהוא כתוב כאן: **סריקה שמאמתת דפוס לפי המפריד שבו הוא הופיע
בפעם הראשונה תמצא רק את המופע הראשון.** ההצהרה "המאגר נקי" נשענה על בדיקה חלשה מכדי
לתמוך בה.

חומר שמור: `דוחות/שכתוב-פירושים/חדש-1-*.md`, `חדש-2-*.md`, `חדש-3-*.md`
(180 שורות, שני מקורות וקישור לכל אחת), `אימות-1.txt`, `אימות-2.txt`, `אימות-3.txt`.

---

## 10. מה שעוד לא נעשה — כדי שהמסמך יישאר כלי ולא תצהיר

- **1,719 הפירושים בעברית לא שוכתבו במלואם.** 62 תוקנו מול מילונים, 251 בסבב מוקדם,
  ו-162 נוסחו מחדש מאפס בשלושה סבבים (סעיף 9). היתר עדיין בנוסחם המקורי. **זו החשיפה הפתוחה
  הגדולה ביותר**, כי הגדרה בפרוזה היא טקסט מוגן בשונה מזוג מילה-תרגום. המדידה:
  מתוך 1,719, **887 פירושים קצרים מ-25 תווים** — רשימות נרדפות שהן עובדה ולא ניסוח —
  ורק **157 ארוכים מ-60 תווים**. היקף החשיפה הממשית הוא בסדר גודל של מאות, לא אלפים.
- **לא נמדד כיסוי מול מקור בלתי-תלוי.** אין עדיין מספר שאומר איזה אחוז מהזוגות
  מאומת במקור ברישיון פתוח.
- **332 פירושים לא שמישים** ו-**169 סידורי משמעות** מדוחות הביקורת טרם טופלו.
- **הפרשנות המשפטית כאן אינה ייעוץ משפטי.** נכתבה על ידי עוזר AI, לא על ידי עורך דין.
  ניתוח מלא של הסיכון, כולל סעיפים 10 ו-18 בתנאי השימוש של Quizlet, נמצא ב-
  `דוחות/זכויות-מקור-המילים.md`.

---

## 10. מדידת כיסוי מול מקורות בלתי-תלויים

**השאלה:** איזה אחוז מזוגות (מילה, תרגום) שלנו מאומת במקור חיצוני עצמאי. זוג מאומת
הוא **עובדה מוסכמת** ולא בחירה מקורית של אף אחד — ולכן סעיף 5 לחוק חל עליו.

**שני עדים, שניהם בשימוש כעד חיצוני בלבד ולא כמקור שממנו נגזר משהו:**

1. **ויקימילון** (`en.wiktionary.org`) — טבלאות התרגום בערך האנגלי.
2. **ויקינתונים — Lexemes** (CC0) — לקסמה אנגלית ⇒ מובן ⇒ הפריט הרעיוני (P5137)
   ⇒ תווית העברית של אותו פריט.

מדגם דטרמיניסטי (ללא `Math.random`, ולכן ניתן לשחזור) של 60 זוגות פרוסים על פני
כל המאגר האנגלי. הסקריפטים: `scratchpad/coverage.js`, `scratchpad/coverage2.js`.
הפלט הגולמי: `דוחות/כיסוי-מקורות-עצמאיים.tsv`, `דוחות/כיסוי-ויקינתונים.tsv`.

| | ויקימילון | ויקינתונים | לפחות אחד |
|---|---|---|---|
| ניתן לבדיקה | 28 | 31 | **39 מתוך 60 (65%)** |
| מאומת | 23 (82%) | 25 (81%) | **32 (82%)** |

**התוצאה במדגם המצומצם: 82%.** לאחר מכן הורחבה המדידה, והמספר ירד:

| מדגם | ניתן לבדיקה | מאומת |
|---|---|---|
| ויקימילון, 200 זוגות | 110 (55%) | 82 = **74.5%** |
| ויקינתונים, 400 זוגות | 201 (50%) | 144 = **71.6%** |

**המספר שיש לעמוד מאחוריו הוא 72–75%, לא 82%.** מדגם בן 60 היה קטן מכדי לתמוך
ב-82%, וההפרש בין המדגמים הוא בדיוק סוג הטעות שמדידה גדולה נועדה למנוע.
הכיסוי עצמו יציב: כמחצית מהזוגות ניתנים לבדיקה, ועל המחצית השנייה שני המקורות
שותקים — לא הפריכו, פשוט אין להם ערך.

**ה-82% הוא הערכת חסר, ואחת הסיבות נמדדה:** מתוך 11 המקרים שסומנו "שונה", 3 אומתו
בהתאמה סובלנית לצורת הפועל, ורוב הנותרים הם הבדל צורני ולא סמנטי — אצלנו
`discover = לגלות` (שם פועל) ואצלם `גילה` (עבר), `relevant = רלוונטי` מול תווית
עברית אחרת לאותו מושג. אף אחד מהם אינו תרגום שגוי.

**תקלה מתודית שנתפסה ותוקנה:** ההרצה הראשונה החזירה 102 כשלים מתוך 120 ונראתה
כהיעדר כיסוי חמור. זו הייתה **חסימת קצב** של ויקימילון ולא היעדר נתונים. נוספו
השהיה של 1.1 שניות בין בקשות ו-retry עם השהיה גדלה. **מספר שנמדד דרך צינור חסום
מודד את הצינור, לא את הנתונים.**

---

## 11. הומוגרפים — למה פיצול לשני ערכים אינו אפשרי, ומה נעשה במקום

שישה ערכים מנו משמעויות ששייכות ל**מילה אחרת** מזו שהניקוד שלהם מציג: `כֶּבֶשׁ`
(שם עצם) מנה משמעויות של `כָּבַשׁ` (פועל), וכן `גִּדֵּם`, `רֶשֶׁף`, `חָפַן`,
`קֶלַח`, `מִקְשָׁה`.

ההנחה הראשונה הייתה שצריך לפצל כל אחד לשני ערכים. **הבדיקה השנייה הראתה שזה
בלתי אפשרי במבנה הנוכחי, ולא היה מתגלה בבדיקה חזותית.** `buildBank` ממפתחת כל
ערך לפי **שלד העיצורים** אחרי הסרת ניקוד:

| | מפתח |
|---|---|
| `כֶּבֶשׁ` / `כָּבַשׁ` | `כבש` |
| `רֶשֶׁף` / `רָשַׁף` | `רשפ` |
| `חָפַן` / `חֹפֶן` | `חפנ` |
| `קֶלַח` / `קִלֵּחַ` | `קלח` |

שני ערכים עם אותו מפתח מקופלים חזרה לערך אחד, והפירוש השני **מצורף** לראשון —
כלומר הפיצול היה מייצר בדיוק את הערך המאוחד שממנו יצאנו, בלי שום סימן לכך.

**מה שנעשה במקום:** כל ערך הותאם לניקוד שלו, והמשמעויות של המילה האחרת הוסרו.
`מִקְשָׁה` נותר מאוחד, כי שם מדובר בהומוגרף אמיתי — שתי מילים בעלות אותו ניקוד
בדיוק — ולכן איחודן אינו שגיאה.

**המחיר, ונאמר כאן במפורש:** המילה השנייה בכל זוג **אינה נמצאת יותר במאגר**.
זו החלטה מודעת — עדיף ערך אחד נכון על ערך אחד שמלמד שתי מילים כאילו הן אחת.
הוספת המילה השנייה תדרוש מפתח שמבחין בניקוד, וזה שינוי במנוע ולא בנתונים.

## 12. ביקורת נתונים נוכחית — הפרדה בין רמת הנתונים לרמת המנוע

`scratchpad/examaudit.js` נבנה מחדש כדי שהמספרים יתייחסו לנתונים של **היום**.
על 5,413 הערכים שנבדקו אז נמצאו **175 בעיות ברמת הנתונים**: 66 פירושים מעגליים,
56 מקרים שבהם שני ערכים חולקים פירוש זהה, ו-53 פירושים שאינם ניתנים לבחינה.

**זו אינה נסיגה מ"אפס הפרות ב-10,225 פריטים" שבסעיף 7, ואסור לבלבל ביניהם.**
סעיף 7 מדד **פריטי מבחן שנוצרו בפועל**, אחרי שמנוע המבחנים סינן פריטים שאי אפשר
לבחון והחריג מסיחים מתנגשים. 175 המקרים כאן הם החומר הגולמי שהמנוע עוקף בזמן
ריצה. הם אינם באגים במבחן — הם פוגעים בתרגול, והם חלק מ-332 הפירושים הלא-שמישים
שדוח הביקורת מנה בסעיף 4.
