# large / copyrighted source PDFs — not needed for the site
*.pdf
יחידות/**/*.pdf

# local tooling
.claude/
scratchpad/

# os
.DS_Store
Thumbs.db

# דוחות עבודה פנימיים — לא לפרסום
# ⚠ התבנית חלה על **כל** תיקייה בשם הזה בכל עומק, ולכן היא מכסה גם `ביקורת/דוחות/`.
# נתפס ב-16.8 כשנבנתה מערכת הביקורת. זו התוצאה שאנחנו רוצים ולכן היא נשארת, אבל
# בכוונה ולא במקרה: דוח ביקורת מפרט באגים ולעיתים נוגע במשתמש מסוים, והריפו ציבורי.
# ⭐ מה שכן בגיט הוא `ביקורת/יומן-עבודה.md` — הוא נקודת התיאום בין סשנים, ולכן
# חייב להיות משותף. הדוחות עצמם מקומיים.
דוחות/

# מסמכי הקמה — נשארים מקומית
supabase-setup.md
migrations/

# רשימת המשימות שלי — פרטית, לא לפרסום
משימות.md

# לוח מצב פנימי - לא לפרסום
מצב-הפרויקט.html

# סיסמאות - לעולם לא במאגר
*סיסמא*
*סיסמאת*

# מפתחות סביבה. הרשימה הזאת מקדימה את הקובץ הראשון מהסוג הזה בכוונה:
# .env נוצר ביום שמחברים ספק תשלומים או Resend, והמאגר הזה פומבי.
.env
.env.*
*.env
!.env.example

# שיווק - נכסים גדולים
# ‏** ולא * : אחרי סידור התיקייה (3.8.2026) הסרטונים יושבים ב-שיווק/סרטונים/, ותבנית
# עם כוכבית אחת מכסה רק את הרמה הראשונה — שני קבצי mp4 בנפח 3.2MB חדלו להיות מוחרגים.
שיווק/**/*.mp4
שיווק/*.mp4

# מסמכי עבודה פנימיים
יועצים.md
דוחות/סדר-משמעויות/

# בחינות נייט — טקסט מלא של מבחנים מוגנים בזכויות יוצרים. לעולם לא במאגר.
בחינות-נייט/

# גרף הידע — נבנה מקומית מהקוד, לא נכס של הפרויקט
graphify-out/

# Remotion · תלויות ופלט רינדור ביניים
שיווק/remotion/node_modules/
שיווק/remotion/out/

# מסמך פנימי לטיפול בטענת מקור המילים. יושב ב-שיווק/ כדי שמנהל השיווק יעבוד איתו,
# אבל שיווק/ עולה למאגר הציבורי — והמסמך מפרט מה לא להגיב ומה לא לשנות. פרסום שלו
# מוסר את ההנחיות לצד השני. נתפס ב-5.8 לפני הקומיט הראשון.
שיווק/תוכניות/סוגיית-מקור-המילים.md

מסמכים/

# גופני הלוח — מגיעים עם סקיל canvas-design, לא צריך לשכפל אותם לריפו
שיווק/עיצוב-אתר/fonts/

# פייתון — פלט קומפילציה, נוצר מחדש בכל הרצה
scripts/__pycache__/
__pycache__/
*.pyc
units_output/gloss-phase/hewiktionary.xml.bz2

# טקסט מלא של מבחנים — לעולם לא במאגר הציבורי (כמו בחינות-נייט/)
exams/normalized/
exams/txt/

# גיבוי מקומי אוטומטי · נוצר בסשן 15-16.8 כשהאינטרנט קרס. עותק של קבצים שטרם
# קומטו, נכתב כל 45 שניות. מקומי בלבד ולעולם לא נכנס לריפו הציבורי.
.snapshots/

# דמואים מקומיים · לא לפרסום
tts-demo.html
blue-demo.html

# משימות פתוחות של סשנים — קובץ אחד לכל סשן, מקומי בלבד
# אותו נימוק כמו `דוחות/`: הקובץ מפרט באגים פתוחים ולעיתים נוגע במשתמש מסוים,
# והריפו ציבורי. ⭐ ולמה זה גם מיותר בגיט: הקובץ נחוץ לסשן **אחד** על מכונה אחת,
# ואין לו שום צרכן אחר. מה שכן משותף בין סשנים נשאר `ביקורת/יומן-עבודה.md`.
# ⚠ הנתיב מפורש ולא `משימות/`, כדי שהתבנית לא תבלע תיקיות אחרות בשם הזה.
ביקורת/משימות/

# דמואים מקומיים · נבנים מחדש בכל פעם ואינם צריכים לשבת בריפו ציבורי
דמו-עיצוב.html
demo-standalone.html
tts-demo.html
blue-demo.html
