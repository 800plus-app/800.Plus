# large / copyrighted source PDFs — not needed for the site
*.pdf
יחידות/**/*.pdf

# local tooling
.claude/
scratchpad/

# os
.DS_Store
Thumbs.db

# דוחות עבודה פנימיים — לא לפרסום
דוחות/

# מסמכי הקמה — נשארים מקומית
supabase-setup.md
migrations/

# רשימת המשימות שלי — פרטית, לא לפרסום
משימות.md

# לוח מצב פנימי - לא לפרסום
מצב-הפרויקט.html

# סיסמאות - לעולם לא במאגר
*סיסמא*
*סיסמאת*

# מפתחות סביבה. הרשימה הזאת מקדימה את הקובץ הראשון מהסוג הזה בכוונה:
# .env נוצר ביום שמחברים ספק תשלומים או Resend, והמאגר הזה פומבי.
.env
.env.*
*.env
!.env.example

# שיווק - נכסים גדולים
# ‏** ולא * : אחרי סידור התיקייה (3.8.2026) הסרטונים יושבים ב-שיווק/סרטונים/, ותבנית
# עם כוכבית אחת מכסה רק את הרמה הראשונה — שני קבצי mp4 בנפח 3.2MB חדלו להיות מוחרגים.
שיווק/**/*.mp4
שיווק/*.mp4

# מסמכי עבודה פנימיים
יועצים.md
דוחות/סדר-משמעויות/

# בחינות נייט — טקסט מלא של מבחנים מוגנים בזכויות יוצרים. לעולם לא במאגר.
בחינות-נייט/

# גרף הידע — נבנה מקומית מהקוד, לא נכס של הפרויקט
graphify-out/

# Remotion · תלויות ופלט רינדור ביניים
שיווק/remotion/node_modules/
שיווק/remotion/out/
