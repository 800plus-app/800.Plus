# 800+ · הוראות פרויקט

## ⛔ כלל עומד · חשיפה משפטית — קודם לכל שיקול אחר

**אסור להכניס לפרויקט הזה שום דבר שאינו חוקי לשימוש, ושום דבר שעלול לחשוף את חגי
לתביעה.** הכלל חל על **כל** סוג נכס, בלי יוצא מן הכלל:

מילים · פירושים · משפטים · שאלות מבחן · עיצוב · גופנים · תמונות · אייקונים · מוזיקה ·
וידאו · קוד · ספריות · נתונים · רשימות תדירות · רעיונות · שמות · סימני מסחר · טקסט שיווקי.

### מה לעשות כשעולה ספק

1. **לעצור לפני הלקיחה**, לא אחריה. חומר שנכנס למאגר ציבורי אי אפשר לבטל.
2. **לאמת מול המקור עצמו** — קובץ הרישיון, לא מה שכתוב אצלנו על הרישיון, ולא מהזיכרון.
   התיעוד הפנימי כבר נמצא שגוי פעם אחת (ראה §רישיונות למטה).
3. **להציג לחגי את המצב**: מה המקור, מה הרישיון המדויק, מה הוא מחייב, ומה החלופה.
4. **לא להכריע לבד.** גם "כנראה בסדר" הוא ספק, וספק מוצג ולא נבלע.

⚠ **ההערכה שלי כאן היא הנדסית ולא משפטית. אני לא עורך דין**, ובכל מקום שבו התשובה
תלויה בפרשנות — זה מה שצריך להיאמר, ולא ניחוש בנוסח של ביטחון.

### שאלות שחוזרות, ומה נבדק בהן

- **תוכן שנוצר על ידי LLM** — מותר, וזה הדפוס של הפרויקט (`METHODOLOGY.md`).
- **CC BY-SA** — מחייב ייחוס **וגם** שיתוף-זהה על יצירה נגזרת. עובדות בודדות
  (מספר תדירות) אינן מוגנות; החשיפה גדלה כשהחילוץ מסיבי.
- **CC BY** — ייחוס בלבד. עדיף על BY-SA לכל שימוש בפרויקט הזה.
- **מקורות אסורים במפורש** (כלל א5): Quizlet · Campus IL · Wiktionary (CC BY-SA) ·
  Hebrew WordNet · האקדמיה ללשון · חומרי הכנה מסחריים · שאלות ממבחני עבר של המכון.
- **אתרים שחוסמים סורקי AI** (פרויקט בן-יהודה, האקדמיה ללשון) — לא נגררים מהם,
  גם כשהתוכן נחלת הכלל.

## רישיונות · מה נמצא בפועל

| נכס | מקור | רישיון | מצב |
|---|---|---|---|
| `enrank.js` · 3,175 דירוגי תדירות | [hermitdave/FrequencyWords](https://github.com/hermitdave/FrequencyWords) ← OpenSubtitles2016/2018 | **קוד MIT · תוכן CC BY-SA 4.0** | ⚠ הקובץ ציבורי **בלי ייחוס ובלי הודעת רישיון**. הייחוס קיים ב-`METHODOLOGY.md` בלבד |

⚠ **`METHODOLOGY.md §5` כותב "CC BY-SA 3.0" וזה שגוי — הרישיון הוא 4.0.** אומת ב-10.8.2026
מול ה-README ומול קובץ ה-LICENSE של המאגר.

⭐ **מה שמחזיק את העמדה:** ה**בחירה** אילו 3,175 מילים לדרג היא שלנו ונגזרה מ-`data-en.js`.
מהם נלקחו **המספרים בלבד**. זה בדיוק מה ש-`METHODOLOGY.md` מנסח כ"מדד חיצוני, לא מקור
שממנו נגזר המאגר", וזו הסיבה שהטענה "חילצנו עובדות" מחזיקה כאן.

**הסדר שחייב להישמר בכל הוספה עתידית:** בוחרים את המילה כי אנחנו צריכים אותה → **ואז**
מחפשים לה דירוג. אם הרשימה החיצונית בוחרת אילו מילים ייכנסו, היא הופכת למקור שממנו
נגזר התוכן, וזה שימוש אחר לגמרי.

## graphify

יש גרף ידע ב-`graphify-out/`. `graphify query "<שאלה>"` לפני חיפוש גולמי.
⚠ נמצא בפועל שהכיסוי חלקי — על שאלות ממוקדות (`LV_CUT`, מקור `EN_RANK`) הוא החזיר
2–41 צמתים בלי תשובה. כשהוא לא מכסה, ממשיכים ל-Grep ואומרים שהוא לא כיסה.

## בדיקות

`node tests/run.js` — **928 בדיקות**. **ההנחה "אין test runner" שגויה** והיא כבר הופיעה
בקובצי משימות. החבילה תפסה רגרסיות אמיתיות.

`node sentence-completion/verify_all.js` — כל השערים של השלמת המשפטים בפקודה אחת, עם
פסק דין אחד: בניית הקורפוס וקובץ הייצור, שער הפורמט (כולל `--selftest` שמוכיח שיניים),
שער הפריטים, מילות הקישור, וחבילת הבדיקות.

⚠ **שער שמדווח "עבר" בלי הוכחת שיניים אינו עדות.** זה קרה שלוש פעמים בפרויקט הזה:
שער שבדק את הדבר הלא נכון (`fix_dashes.js`), תנאי שקרס לשקר תמיד ולכן דגל שמעולם לא
נורה (`check_explain.js`), ושער שירה 184 דגלים שרובם שקריים. מכאן: שער חדש מגיע עם
הרצה על מקרה שאמור להיפסל.

## ⛔ יומן העבודה · חובה בסוף כל משימה

**בסוף כל משימה שנסגרה — שורה ב-`ביקורת/יומן-עבודה.md`.** לא בסוף היום, בסוף המשימה.

```
| ⬜ | <תאריך> | <קומיט> | <מה נעשה> | <קבצים> | <איך אימתתי> | <מה לא בדקתי> |
```

⭐ **שני השדות האחרונים הם כל הערך של היומן**, והם אלה שנוטים להישאר ריקים:

- **«איך אימתתי»** — הפקודה והתוצאה. `1,035 בדיקות עוברות` הוא אימות;
  `בדקתי שזה עובד` אינו.
- **«מה לא בדקתי»** — זה מה שמפנה את המבקר לנקודה הנכונה במקום לחפש באפלה.
  ⛔ **סשן שכותב "הכול נבדק" מבזבז לו את הסבב**, וזה כמעט תמיד לא נכון.

⛔ **ואל תסמן ✅ בשורה שלך.** רק הסשן המבקר מסמן. סשן שמאשר את עצמו הוא כל הבעיה
שהמערכת הזאת נבנתה בשבילה — הוא מכיר את ההנחות שלו, ולכן עיוור בדיוק לנקודות שבהן
הן שגויות.

## ⛔ משימות להמשך · חובה בסוף כל סשן

**היומן הוא מה שנסגר. זה מה שלא.**

**בסוף כל סשן — כל מה שנשאר פתוח אצלך הולך ל-`ביקורת/משימות/`**, בקובץ ששייך רק לך:

```
ביקורת/משימות/YYYY-MM-DD-<נושא>-<8 התווים הראשונים של מזהה הסשן>.md
```

⭐ **המזהה נגזר מנתיב ה-scratchpad שלך עצמך**, ולכן אתה מחשב את שם הקובץ לבד ואין
שני סשנים שיכולים לקבל את אותו שם:

```
…\Temp\claude\C--Users-03hag\bc9ac2df-90bb-457c-a87b-216d0538250e\scratchpad
                             └──────┬──────┘
```

⛔ **שלושה כללים:** כותבים **רק** לקובץ שנגמר במזהה שלך · יש כבר קובץ כזה? **מוסיפים
לו, לא יוצרים חדש** · **אין קובץ אינדקס משותף** — רשימת התיקייה היא האינדקס, וכל קובץ
מרכז הוא בדיוק נקודת ההתנגשות שהמבנה בא למנוע.

⭐ **לכל משימה חייב להיות «המדד שסוגר»** — מבחן שיכול להיכשל.
`node tests/run.js מחזיר 1,036` הוא מדד · `לוודא שזה עובד` אינו. משימה בלי מדד אינה
משימה, היא כותרת — וזו בדיוק הסיבה שהיא תיתקע שוב בסבב הבא.

**התבנית:** `ביקורת/משימות/_תבנית.md` · **הכללים המלאים:** `ביקורת/משימות/README.md`.

**המערכת המלאה:** `ביקורת/README.md`. **הפרומט של המבקר:** `ביקורת/מבקר-פרומט.md`.
