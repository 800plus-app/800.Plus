# ‏gloss · נקודת ההפעלה הטובה ביותר באפס קבלות-שווא

נוצר 2026-08-15T02:16:45.085Z · `node typo-lab/gloss_search.js --selftest` · 9816973 הערכות · 640.9s

## התשובה בשורה אחת

אפס קבלות-שווא שהגנום אחראי להן, על **כל** השורות (28880 שורות סט · 24618 אבולוציה + 4262 holdout · ועוד 12 שליליות חוצות-כרטיסים): **11.61%** holdout recall · 12.46% על שורות האבולוציה · ‏recallIncludingUntrusted 9.45% ב-holdout ו-9.85% באבולוציה.

הנקודה שנכתבה ל-`out/gloss-candidate-rules.json` היא **A_allRows**. השוואה: השנשלח **0.00%**, ו-`coverage.js` מדדה **8.61%** holdout (בכיול רצועות-אורך, על גרסה קודמת של `typo-rules.json` שבה למשקלי ה-gloss היו ערכים אחרים לגמרי · `del 0.2`, `homophone 0.2`). כאן המשקלים נסרקו במפורש, ולכן המספר גבוה יותר.

⚠ **הספירה המילולית של `evolve.js` אינה יכולה לרדת מ-2**, ולא בגלל כיול: 1 שורה בסט מתויגת `reject` ובכל זאת **השכבה הראשונה מקבלת אותה כבר היום**. ‏`evalSubset` סופרת אותה ב-`fa`, ואף פרמטר אינו יכול לגרום לה או למנוע אותה. כל המספרים כאן מדווחים בשתי הספירות בנפרד.

- `"כל"` ~ `"כלל"` · כרטיס `rule|2` · `why=ambiguous:כבל` · `op=drop` · fold 1 · holdout false

## הנקודות שנמדדו

| נקודה | מרחב | minLen | שוליים | evolve | holdout | ‏faOwn סה"כ | ‏fa בספירת evolve.js |
|---|---|---|---|---|---|---|---|
| A · אילוץ על הכול, מטרה holdout | perLen | 6 | 2 | 12.46% | 11.61% | 0 | 2 |
| B · אילוץ ומטרה על אבולוציה בלבד | perLen | 8 | 2 | 14.94% | 13.93% | 2 | 4 |
| C · מועמד למשלוח | perLen | 8 | 2 | 11.25% | 11.03% | 0 | 2 |
| D · מרחב הגנים של ה-GA | ga12 | 6 | 1 | 10.73% | 9.96% | 0 | 2 |

הפרמטרים שנשלחו · evolve 0.00% · holdout 0.00% · כולל לא-מהימנות 0.00% · faOwn 0 · fa 2

### קבלות-שווא לפי אוכלוסייה · הנקודה הנבחרת

- שורות אבולוציה (24618) · קבלות-שווא של הגנום **0** · ועוד 1 משכבה 1 שאינן בשליטת אף פרמטר
- ‏holdout (4262) · קבלות-שווא של הגנום **0** · ועוד 0 משכבה 1
- שליליות חוצות-כרטיסים · **0** מתוך 12 שורות · הקבוצה ריקה לסט הפירושים (`buildCrossCard` שמרה 0 זוגות משני הצדדים · he/gloss ו-en/gloss), ולכן האילוץ הזה **אינו** מה שחסם את gloss
- ‏real-word · 0 קבלות-שווא על שליליות קשות, על כל השורות

### מעבר הכיווץ · הורדת כל רצועה לערך הקטן ביותר שאינו משנה דבר

פינת התיבה נותנת את ה-recall המרבי, אבל היא נותנת אותו עם רצועות שיושבות על תקרת הרשת (`3`) רק מפני שאף שורה בדאטהסט אינה מגבילה אותן. רצועה כזאת **לא הופרכה**, וזה אינו אותו דבר כמו "בטוחה": היא נעשית חיה ברגע שמישהו מוסיף כרטיס למאגר. לכן כל רצועה הורדה לערך הקטן ביותר ברשת ששומר על **קבוצת ההכרעה זהה בדיוק** · אותן שורות מתקבלות, אותן נדחות, בכל הסט ובשליליות. רצועה שאף קבלה אינה עוברת דרכה יורדת בכך ל-`0` מעצמה.

| ‏maxLen | לפני | אחרי |
|---|---|---|
| 1 | 3 | **0** ← |
| 2 | 3 | **0** ← |
| 3 | 3 | **1.8** ← |
| 4 | 0.6 | **0.4** ← |
| 5 | 0.15 | **0** ← |
| 6 | 0.15 | **0** ← |
| 7 | 0.45 | **0.4** ← |
| 8 | 0.45 | **0.4** ← |
| 9 | 0.15 | **0** ← |
| 10 | 0.55 | **0.5** ← |
| 11 | 1.85 | **1.8** ← |
| 12 | 0.55 | **0.5** ← |
| 13 | 0.55 | **0.5** ← |
| 14 | 1.95 | **1.9** ← |
| 15 | 1.85 | **1.8** ← |
| 16 | 1.15 | **1.1** ← |
| 17 | 3 | **2** ← |
| 18 | 3 | **2** ← |
| 19 | 3 | **2** ← |
| 20 | 3 | **0.6** ← |
| ∞ | 3 | **0** ← |

21 רצועות ירדו, 6 מהן ל-`0` (`maxLen` 1, 2, 5, 6, 9, ∞) · 2 סבבים עד נקודת שבת.

**מה כל רצועה נושאת.** מאפסים רצועה אחת וסופרים כמה קבלות אמת נעלמו · לפי פירוק האיחוד זו התרומה הייחודית שלה. 15 רצועות מתוך 21 חיות:

| ‏maxLen | ‏t אחרי הכיווץ | קבלות אמת שהיא לבדה נושאת · evolve | ‏holdout |
|---|---|---|---|
| 3 | 1.8 | 3 | 0 |
| 4 | 0.4 | 1 | 0 |
| 7 | 0.4 | 81 | 7 |
| 8 | 0.4 | 57 | 14 |
| 10 | 0.5 | 124 | 14 |
| 11 | 1.8 | 179 | 28 |
| 12 | 0.5 | 44 | 8 |
| 13 | 0.5 | 32 | 11 |
| 14 | 1.9 | 88 | 13 |
| 15 | 1.8 | 59 | 5 |
| 16 | 1.1 | 27 | 7 |
| 17 | 2 | 26 | 6 |
| 18 | 2 | 14 | 3 |
| 19 | 2 | 2 | 4 |
| 20 | 0.6 | 2 | 0 |

⚠ **ממצא · הרצועות הקצרות אינן מתות.** ההנחה שלי בסבב הקודם הייתה ש-`maxLen` 1–4 בלתי-נגישות מאחורי `minLen 6`. הכיווץ הפריך אותה: `3`, `4` ירדו אבל **לא** ל-0, כי איפוסן מוחק קבלות אמת. הסיבה: `minLen` מסנן את אורך המחרוזת ש**הוקלדה**, ואילו הרצועה נבחרת לפי אורך ה**מועמד**; עם `del 0.2` מחיקה זולה, מחרוזת מוקלדת ארוכה מיושרת למקטע פירוש קצר בעלות נמוכה. ‏`maxLen` 1 ו-2 אכן יצאו 0; 3 ו-4 לא. זה בדיוק סוג הרצועה שראוי שהשער הממצה יסתכל עליה.

**והמספרים לא זזו.** לפני: evolve 12.46% (tp 739) · holdout 11.61% (tp 120) · faOwn 0. אחרי: evolve 12.46% (tp 739) · holdout 11.61% (tp 120) · faOwn 0. זהות **סיבית-סיבית** ✅ · לא "בערך".

הארטיפקט שנכתב הוא **המכווץ**. כל מספר בדוח הזה נמדד עליו, כולל המסלול המדויק למטה.

### ‏recall לפי אורך המפתח · הנקודה הנבחרת

| אורך | ‏accept | ‏tp | ‏recall | ‏reject | ‏fa |
|---|---|---|---|---|---|
| 2 | 0 | 0 | — | 12 | 0 |
| 3 | 106 | 0 | 0.00% | 2589 | 1 |
| 4 | 824 | 0 | 0.00% | 5944 | 0 |
| 5 | 1641 | 2 | 0.12% | 5457 | 0 |
| 6 | 996 | 2 | 0.20% | 2225 | 0 |
| 7 | 815 | 88 | 10.80% | 843 | 0 |
| 8 | 586 | 71 | 12.12% | 550 | 0 |
| 9 | 622 | 0 | 0.00% | 564 | 0 |
| 10 | 454 | 138 | 30.40% | 388 | 0 |
| 11 | 290 | 207 | 71.38% | 266 | 0 |
| 12+ | 629 | 351 | 55.80% | 1261 | 0 |

### הנקודה העיוורת שהשער חשף · תוצרי ההרחבה של B1

המועמד הקודם נפל ב-`bank_gate` על שתי התנגשויות, ושתיהן דרך אותו ערוץ:

```
⛔ [he/gloss] "רסיס עצ" accepted on "אֵגֶל" · belongs to שבב
⛔ [he/gloss] "משא כבד" accepted on "יָצוּעַ" · belongs to עול
```

`"רסיס עצ"` הוא **תוצר הרחבה** של הכרטיס `שבב` תחת חוק `B1-union` (פיצול "או" מחלק), ולא מקטע פירוש גולמי. מכאן שתי העיוורויות בו-זמנית: הוא אינו באינדקס המקטעים ולכן **שולי הדו-משמעות אינם רואים אותו** (`dOther` יצא 9, כלומר "אין שכן"), והוא לא היה במפת הבעלים של `buildCrossCard` ולכן **קבוצת השליליות לא הכילה אותו**. ‏"‏0 שורות חוצות-כרטיסים ב-gloss" לא היה סימן לבטיחות · הוא היה סימן לנקודה עיוורת, וכך אמרתי עליו בטעות בסבב הקודם.

המסלול עצמו: `"רסיס עצ"` → `"רסיס"` (מקטע של `אֵגֶל`) הוא **שלוש מחיקות**, ובעלות `del 0.2` זה `dist 0.6` · מתחת לסף של רצועת `maxLen 4`. זו בדיוק אותה רצועה קצרה שהכיווץ בסבב הקודם סירב לאפס, ואותו `del` זול.

**התיקון:** קבוצת השליליות נבנית מחדש כשקבוצת הצורות כוללת את תוצרי ההרחבה של כל כרטיס · 660 צורות he ו-2 צורות en שלא היו שם קודם. הפונקציה עצמה מיובאת מ-`bank_gate.expandOf` עם אותו `cfg`, ולא נכתבת מחדש. סט השליליות של gloss עבר מ-**0** שורות ל-**12**.

ולפני שנבנה עליה משהו · היא נבדקה מול מה שהשער כבר תפס:

- ✅ `"רסיס עצ"` מול `אֵגֶל` · פולש `שבב` · `dOwn 3` · `dOther 9`
- ✅ `"משא כבד"` מול `יָצוּעַ` · פולש `עול` · `dOwn 3` · `dOther 9`

### שכפול השער מקומית · לפני שמבזבזים עוד 504 שניות

‏`glossSweep` אינה מיוצאת מ-`bank_gate`, ולכן לולאת הסריקה נכתבת ב-`lib/gloss_search_expand.js` · אבל היא **מכוילת ולא מוצהרת**: מריצים אותה על הפרמטרים שנפלו בשער בפועל, והיא חייבת למצוא בדיוק את אותן שתי התנגשויות בשמן (שער S10). היא אכן מוצאת אותן, עם `dist 0.6` בשתיהן.

על המועמד הנוכחי: **5,940,613** זוגות · 3,096,785 הוכרעו · **0 התנגשויות חדשות** · 668 קבלות `via=exact` שהן התנהגות קיימת ואינן פסק אדום. 205s.


‏`depth` בשכפול הוא `MAX_OPS` (=3) ולא `effOps(P)` · על-חסם, כי קבלה פאזית דורשת מרחק גולמי `<= MAX_OPS` לכל `P`. לכן השכפול מכריע **על-קבוצה** של מה שהשער מכריע, ואינו יכול לפספס התנגשות `typo`. ההפרש הקטן בספירת הזוגות מול השער הוא קבוצת ה-`always` של השער · צורות שחולקות גזע עמוק ונכנסות להכרעה בלי קשר למרחק, וכולן מגיעות לערוץ `via=exact` ולא ל-`typo`.

**עלות הריצה הבאה שלך.** ‏`EFF` של המועמד הוא **3** · זה מה שקובע את עומק אינדקס-המחיקות בשער ולכן את זמן הריצה. ‏`EFF` נגזר מ-`floor(max(t)/min(W))` חסום ב-`MAX_OPS`, ועם סף חיובי כלשהו ומשקל מינימלי `0.2` הוא נשאר 3. כלומר סדר הגודל של 5,940,613 זוגות ו-~500 שניות הוא **מחיר מובנה של סובלנות חיובית ב-gloss**, ולא משהו שהמועמד הזה החמיר. ‏gloss בסובלנות אפס נותן `EFF=0` ו-15,768 זוגות · זו העסקה.

⚠ זהו **תנאי הכרחי ולא פסק דין**. `bank_gate` בודק גם את שכבת חוק צד-הפירוש (`gloss-rule`), את כיוון המונח, את הנרדפות ואת זוגות הצירה. הוא נשאר הסמכות.

### אימות במסלול המדויק · `lib/checker.js` עצמו

‏`exactEval` הריצה את הבודק האמיתי (`makeChecker/acceptGloss`, כולל מה שהקירוב המהיר אינו רואה) על כל 28880 שורות ה-gloss עם הפרמטרים שנכתבו: **recall 12.34%** · `fa 1` · מתוכן **0** שהגנום אחראי להן · `real-word 0`. הקירוב המהיר נתן 12.34% על אותן שורות · ההפרש נובע מקיצוץ קבוצת המועמדים ל-`CAND_K` בקירוב, וכיוונו לטובת המסלול המדויק.

- `"כל"` ~ `"כלל"` · `rule` · `why=ambiguous:כבל` · `via=exact` ← המסלול המדויק, לא סובלנות

## למה נשלח 0.00%

- `gloss/fold0` · 23 דורות · bestEver `-1000000` · אף פרט קביל לא נמצא
- `gloss/fold1` · 130 דורות · bestEver `0.13139444443751602`
- `gloss/fold2` · 23 דורות · bestEver `-1000000` · אף פרט קביל לא נמצא
- `gloss/fold3` · 23 דורות · bestEver `-1000000` · אף פרט קביל לא נמצא
- `gloss/fold4` · 23 דורות · bestEver `-1000000` · אף פרט קביל לא נמצא
- `gloss/final` · 23 דורות · bestEver `-1000000` · אף פרט קביל לא נמצא

שחזור `gloss/fold1` (אותו זרע, אותו מפרט GA) זהה ליומן דור-דור: **כן**. שתי קבלות-השווא שלו בוולידציה:

- `"כל"` ~ `"כלל"` · `rule|2` · `why=ambiguous:כבל` · `op=drop`
- `"כילי שייט"` ~ `"כלי שייט"` · `craft|5` · `why=real-word` · `op=mater`

האם הנקודה שלנו מקבלת אותן:

- `"כל"` ~ `"כלל"` · **כן**
- `"כילי שייט"` ~ `"כלי שייט"` · לא

## פסק הדין · שלוש האפשרויות, ומה הראיות אומרות

**1 · תכונה מבנית של הנתונים?** לא. תחת האילוץ הקשה של אפס קבלות-שווא שהגנום אחראי להן, על כל השורות שיש, נמדד **11.61%** holdout recall · והוא חיובי, לא אפס. יתרה מזו, גם **בתוך מרחב הגנים המדויק של ה-GA** (שתים-עשרה רצועות) נמדד 9.96%. הייצוג לא היה המחסום.

**2 · אילוץ אפס קבלות-שווא שאי אפשר לספק?** רק בקריאה המילולית, ובגלל שורה אחת. `evalSubset` סופרת ב-`fa` גם קבלות של **השכבה הראשונה** (`meaningMatch`), ובסט הפירושים יש בדיוק 1 שורה כזאת שמתויגת `reject`. כל עוד היא בתת-הקבוצה שהכושר מודד, `fitnessOf` מחזיר `-1e6` לכל גנום · כולל לגנום אפס-הסובלנות. זו **לא** אי-ספיקות של האילוץ הפדגוגי: אף פרמטר אינו יכול לגרום לקבלה הזאת או למנוע אותה. ‏`coverage.js` כבר עשתה את ההבחנה הזאת ומדדה `faOwn` ולא `fa`; `evolve.js` לא.

**3 · כשל חיפוש?** כן · וזה השורש. הרצף מוכח מתוך הארטיפקטים:

- השורה התקועה נמצאת ב-`fold 1` ואינה ב-holdout. לכן היא **בתוך** קבוצת האימון של folds 0,2,3,4 ושל `final`, ו**מחוץ** לקבוצת האימון של fold1 בלבד.
- זה בדיוק מה שהיומן מראה: `gloss/fold1` הוא היחיד שהגיע לכושר חיובי; כל השאר ו-`final` נעצרו על `-1000000` = `-1e6 × 1` · כלומר קבלת-שווא **אחת** בדיוק, לא יותר.
- כשכל גנום מקבל את אותו ציון, נוף הכושר שטוח. `runGA` מעדכן `bestEver` רק ב-`>` ממש, ולכן `sinceImprove` עולה מדור לדור עד `patience=22` והריצה נעצרת ב-23 דורות · וזה בדיוק מספר הדורות שנרשם בארבעת ה-folds וב-`final`.
- הגנום ש"ניצח" הוא הפרט הראשון באוכלוסייה עם הכושר המקסימלי בדור 0, כלומר `SEED_PARAMS[0]` · זרע אפס-הסובלנות. ואכן `params.gloss` שנשלח זהה לו: `minLen 0`, `vetoMargin 1`, כל שנים-עשר הספים 0, ומשקלי ברירת המחדל של `normalizeParams`.

כלומר "רדום מבנית" מתאר נכון את הפלט ולא נכון את הסיבה. הפלט הוא **ברירת המחדל של כשל**, לא תוצאה של חיפוש · ה-GA מעולם לא דירג שני גנומים שונים זה מול זה בסט הזה.

### התיקון המינימלי שהיה מונע את זה

לספור ב-`fitnessOf` את `faOwn` במקום `fa` (או לסנן מהדאטהסט את השורות שהשכבה הראשונה כבר מקבלת · `gen_dataset` אמורה לעשות זאת, וההודעה `⚠ 1 שורות שהחוק של היום כבר מקבל` נדפסת בכל טעינה). זה לבדו מחזיר לנוף הכושר שיפוע, ולפי המדידה כאן היה מאפשר ל-GA להגיע לסביבות 9.96% במרחב הגנים שלו.

## מגבלות · מה המספרים האלה אינם

- ‏**מעגליות.** התיוג עצמו נגזר מאותו לקסיקון שנמדד כאן: `why=real-word` ו-`why=ambiguous` נקבעו מול המאגר והלקסיקון, ואותו לקסיקון הוא גם שכבת הווטו שחוסמת שורות בזמן ההערכה. לכן "אפס קבלות-שווא" הוא אפס **מול התיוג הזה**, ומחרוזת שהלקסיקון אינו מכיר אינה יכולה להיספר. הנקודה שבה זה נושך חזק במיוחד: רצועות אורך שבהן ה-recall קופץ הן בדיוק אלה שבהן הלקסיקון חוסם הרבה שליליות · הרווח שם עשוי להיות רווח של כיסוי מילון ולא של כיול.
- ‏**ה-holdout אינו עצמאי** בנקודה A ובנקודה C: האילוץ נמדד גם עליו. רק נקודה B מכיילת בלי לגעת בו, וההפרש בין B ל-A הוא גודל ההתאמה-לתוך-המדגם.
- ‏**שכבת ההרחבה B1 · הייתה בלתי ממודלת, וזה בדיוק מה שהפיל את המועמד הקודם.** עכשיו היא בתוך קבוצת השליליות (`lib/gloss_search_expand.js`) ובתוך שכפול הסריקה, ושתיהן מכוילות מול הכשל שהשער דיווח. מה שעדיין **אינו** ממודל כאן: שכבת חוק צד-הפירוש כערוץ קבלה של הכרטיס עצמו (`gloss-rule`), כיוון המונח, הנרדפות וזוגות הצירה. `bank_gate.js` נשאר הסמכות היחידה.
- ‏**מטמון שאינו ממופתח בקוד הוא מלכודת.** `out/coverage-cross.json` נכתב ב-01:10, `evolve.js` נערך ב-01:39, ו-`typo-rules.json` מ-02:07 מדווח `rowsInRisk` 534/396 בעוד המטמון מחזיק 533/398. מפתח ה-sha של הדאטהסט לבדו אמר "תקף". המטמון של הקובץ הזה ממופתח בדאטהסט **ובגיבוב כל קובץ שמשתתף בבנייה**, והשוואת המספרים האלה היא שער (S11).
- החיפוש על הספים **מדויק** (פינת התיבה · הוכחה נמדדת בשלב 5). החיפוש על המשקלים הוא ירידת-קואורדינטות ואינו מובטח גלובלית.
- ‏**רצועות שאין להן נתונים · טופל.** בפינת התיבה רצועות שאף שורה אינה מגבילה יושבות על תקרת הרשת. מעבר הכיווץ מוריד כל אחת מהן לערך הקטן ביותר שאינו משנה את ההחלטה, ולכן רצועה בלתי-נגישה יוצאת `0` ולא `3`. מה שנשאר מעל 0 הוא **הדוק** · צעד רשת אחד למטה כבר שובר את זהות ההחלטה, וזה נמדד (שער S9ב). זה מקטין את החשיפה אבל אינו מבטל את הצורך ב-`bank_gate.js`, שרואה גם את שכבת ההרחבה B1 ואת המאגר המלא.

שורת תקציב · **לא התשובה**: עם 5 קבלות-שווא מותרות, holdout 23.69%. האילוץ הקשה נשאר אפס.
