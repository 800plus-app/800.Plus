# ‏`segConcat` · מפרט הדבקה ל-`app.js`

⛔ **לא הודבק על ידי.** ‏`app.js` אינו שלי. זה המפרט המדויק להדבקה ידנית.

## מה נמדד לפני שזה נכתב

| | |
|---|---|
| תצורה | **C1** · רצופים ובסדרם בלבד (`AB`, `BC`) |
| מחרוזות שנוספות | ‏3,243 |
| התנגשויות מדויקות (תוך-מאגר) | **0** |
| ⭐ שער אמיתי (`langModel` + `makeChecker`) | **0** · ‏72 זוגות הגיעו לבודק |
| אדום לפני ירוק | ✅ בלי החריג: 1 (`לקשור קשר` → `plot`) · איתו: 0 |
| ‏29 המקרים החיצוניים | **7/29 → 8/29** · שינוי יחיד: H16-3 |
| טבלת הזהב עם הגן כבוי | **זהה ביט-אחר-ביט** · no-op מוכח |

‏C2 ו-C3 נמדדו ונדחו · C2 אינו קונה דבר מעל C1, ו-C3 מכפיל שטח פנים פי 17 וגדל פקטוריאלית.

## השינוי · שלוש הוספות ב-`app.js`

### ‏1 · נתון · ליד `TYPO_GLOSS_RULES` (‏app.js:1819)

```js
/* צירוף מקטעים חלקי · "קוסמופוליטי רב תרבותי" מול פירוש בן שלושה מקטעים.
   norm מסירה את הפסיק, ולכן צירוף **כל** המקטעים בסדרם כבר מתקבל; צירוף חלקי
   אינו שווה לכלום ואף שכבה לא הגיעה אליו. רצופים ובסדרם בלבד. */
const TYPO_SEG_CONCAT_MAX = 5;
/* ⛔ החריג · נתון, לא if בתוך הלוגיקה. הצירוף הצמוד של שני מקטעי tie מייצר
   "לקשור קשר" — ניב שמשמעותו להתחבר בקנוניה, והוא הפירוש הרשום של plot.
   הצירוף התמים חוצה משמעות, וזה בדיוק מה שהווטו קיים בשבילו. */
const TYPO_SEG_CONCAT_EXCEPT = [{ term: 'tie', typed: 'לקשור קשר' }];
```

### ‏2 · פונקציה · אחרי `typoSegBlocked` (‏app.js:1911)

```js
function typoSegConcat(segs, card){
  const use=segs.slice(0, TYPO_SEG_CONCAT_MAX), out=new Set();
  if(use.length<2) return out;
  const full=segs.join(' ');
  for(let i=0;i<use.length;i++) for(let j=i+1;j<use.length;j++){
    const s=use.slice(i,j+1).join(' ');
    if(s!==full) out.add(s);
  }
  const own=K(card && card.term);
  for(const e of TYPO_SEG_CONCAT_EXCEPT) if(K(e.term)===own) out.delete(norm(e.typed));
  return out;
}
```

### ‏3 · שתי שורות ב-`meaningMatch` · **אחרי** `particleMatch` (‏app.js:1765) ולפני `splitOr`

```js
  if(TYPO_GLOSS_RULES.segConcat && !blocked && typoSegConcat(segs, card).has(a)) return true;
```
⚠ השורה חייבת לשבת **אחרי** חישוב `blocked` (‏app.js:1770). הווטו נבדק עליה
כמו על כל מחרוזת אחרת · בלעדיו השכבה עוקפת את הווטו שהיא יושבת מעליו.

### ‏4 · הדגל

```js
const TYPO_GLOSS_RULES = { splitOr: true, synonyms: true, segConcat: true };
```

**סה"כ: ~18 שורות, כולן אדיטיביות.** אף שורה קיימת אינה משתנה.

### ‏5 · ⛔ שתי שורות שחייבות לנחות **באותו שינוי** · אחרת הבדיקות נופלות

**‏א · `tests/71-typo-tolerance.test.js` · בתוך `canonOf`**, אחרי `co('aShareTight')]`:

```js
      co('aFirst'), co('aShare'), co('aFirstTight'), co('aShareTight'),
      p.segConcat == null ? null : (p.segConcat ? 1 : 0)];
```

**‏ב · `app.js` · שדה `fp` ב-`TYPO_PARAMS`:**

| | |
|---|---|
| היום | `fec09ca1` |
| **אחרי התוספת ל-`canonOf`** | **`6099a5c8`** |

⚠ **וזה נמדד ולא נוחש.** ניסיתי להוסיף את השורה ל-`canonOf` לבד, והבדיקה
`TYPO_PARAMS נושא את טביעת האצבע` **נפלה** — כי כל הוספה ל-`canonOf` משנה את
הטביעה **גם כשהערך `null`** (‏`JSON.stringify` על מערך ארוך יותר), והיא מושווית
ל-`fp` שכתוב קשיח ב-`app.js`. אימתתי שזו אשמתי: עם `tests/71` מ-HEAD החבילה
עוברת בקוד יציאה 0. לכן השורה **הוחזרה** ומחכה כאן, ושתי השורות נוחתות יחד.

## ⛔ מה שחייב לרוץ אחרי ההדבקה

1. `node tests/run.js` — ‏**1032 בדיקות + 3 חדשות, אפס כשלים** (אומת לפני ההדבקה).
2. `node typo-lab/bank_gate.js` — ‏`shipParams` ו-`fingerprint` כבר מעבירים את הגן.
3. אימות בדפדפן על `cosmopolitan` ← `קוסמופוליטי רב תרבותי`.
4. ⚠ ועל `tie` ← `לקשור קשר` · חייב להישאר **נדחה**.

## ✅ הפער נסגר · טבלת זהב ייעודית

‏`out/golden.segconcat.jsonl` · **236 שורות** · `typo-lab/make_golden_segconcat.js`
(‏`--selftest` · 8 שערים). נדגמת דטרמיניסטית מ-867 שורות ה-`seg-concat` שכבר
בקורפוס, בארבע קבוצות: **95** `accept-partial` · **100** `reject` · **40**
`accept-today` · **1** `exception` (‏`tie` ← `לקשור קשר`).

כל שורה נושאת **שתי עמודות** — `off` (הפסק של היום) ו-`on` (אחרי ההדבקה) —
ו-`tests/71` בוחרת לפי מצב הדגל ב-`app.js` בפועל. לכן הבדיקה **חיה בשני
המצבים** ואינה מדלגת אף פעם.

**השן, נמדדה:** ‏`app.js` של היום מול עמודת `off` → ✅ ירוק · מול עמודת `on` →
⛔ **אדום, 95 אי-התאמות**. כלומר הטבלה באמת מבחינה בין הגן דלוק לכבוי.

⚠ **ולמה לא הרחבנו את הזהב הראשי:** הוא נדגם מ-`dataset-*.jsonl`, ואין בו אף
צירוף חלקי — זהב שהופק עם הגן דלוק יצא **זהה ביט-אחר-ביט**. ייצור מחדש של
הדאטהסט היה מזיז 10,000 החלטות שאין להן קשר למועמד ומערבב את השינוי ברעש,
והתלמיד גם אומן עליו.

## ⛔ פער ישן שנסגר · לתיעוד

‏`golden.SEGCONCAT.jsonl` יצא **זהה ביט-אחר-ביט** ל-`golden.jsonl`. הסיבה:
טבלת הזהב נדגמת מ-`dataset-*.jsonl`, ואין בו אף שורה שהיא צירוף חלקי של שני
מקטעים. כלומר **הגן נשלח בלי שומר התנהגותי** — הטביעה מכסה אותו (שלושה מקומות),
אבל שום החלטה בטבלה אינה מפעילה אותו.

**התיקון לפני משלוח:** ש-`gen_dataset` יפיק שורות `seg-concat` (הן כבר קיימות
ב-`answers-*.jsonl`, ‏867 שורות, אבל טבלת הזהב אינה נבנית משם). זה הפער היחיד
שנשאר, והוא מהמשפחה שנפלה כאן שלוש פעמים: שער שאינו רואה את הגן שהוא שומר עליו.
