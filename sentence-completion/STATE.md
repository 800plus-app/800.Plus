> ⚠ מיושן (הוקפא 14.8). האמת: C:\Users\03hag\Claude projects\החלטות-800plus.md + ביקורת\החלפת-מנהל.md

= מצב השלמת המשפטים · 11.8.2026

מסמך המצב. נכתב כדי שאיבוד הקשר לא יעלה דבר: מי שקורא אותו יודע מה קיים, מה
נמדד, ומה פתוח, בלי לקרוא את השיחה.

## הקורפוס · באוויר

**450 פריטים** · בסיס 50 · בינוני 100 · מתקדם 150 · אקדמי 150 (היעד בדיוק).
`REV 183` · `origin/main` = `8d8622f`.

מבנה פריט: `s` המשפט עם `___` · `o` ארבע אפשרויות · `a` **תמיד 0** · `k` סוג
היגיון · `w` מילת קישור · `g` פירוש כל אפשרות · `t` תרגום המשפט לעברית כשהתשובה
מושלמת ומודגשת ב-`**…**` · `r` נימוק לכל אפשרות.
⚠ `e` הוא שדה הפורמט **הישן** וקיים רק ב-204 הראשונים. אינו נדרש בפריט חדש, אינו
נשלח לדפדפן, ואינו מוצג ללומד.

⛔ **`a` הוא 0 בכל 450 הפריטים.** האפליקציה **חייבת** לערבב, והיא עושה זאת
ב-`sentShuffled` — שממפה מחדש `o` ו-`g` ו-`r` **יחד**. מיפוי של האפשרויות בלבד
מצמיד לכל מילה את הפירוש של מילה אחרת, וזה כשל שקט.

## הפקודות

```
node sentence-completion/verify_all.js          כל השערים, פסק דין אחד
node sentence-completion/check_requirements.js  13 הדרישות של חגי, כל אחת בשמה
node sentence-completion/check_structure.js     כשלים מבניים (זוג נרדפים, תווית)
node sentence-completion/band_words.js          מחדש את רשימות המילים לכותבים
node tests/run.js                               929 בדיקות
```

⚠ **`verify_all` בונה מחדש בשלב הראשון.** שער שרץ על תוצר מיושן הוא כשל שקרה
בפועל: תיקנתי שלוש שאריות במנות, השער המשיך לדווח עליהן, כי הוא קורא את
`data-sent-en.js` ולא בניתי מחדש.

## המדידה · מה נמדד ומה לא

`verify_all` מוכיח **מבנה**. איכות התוכן נמדדת בבודקים עצמאיים שאינם רואים את
המפתח, והתוצאות ב-`runs/`.

| | 204 הראשונים | 246 החדשים |
|---|---|---|
קלוז TOP1 | 71% | **77%** |
קלוז MISS | 7% | 11% |
רב-ברירה, ניחושים | 16 | **2** |
אדוורסרי | 47 FAIL | **13 FAIL** · 206 נקיים |

⭐ **בכל 28 הפריטים שנפסלו בקלוז ב-246 החדשים, המילה שהפותרים העדיפו אינה בין
ארבע האפשרויות.** אפס שתי-תשובות-נכונות. `score450.js` עושה את ההבחנה בעצמו.

⚠ MISS עלה מ-7% ל-11%: הרצועות החדשות קשות יותר, ובמילים נדירות יש יותר נרדפים
טבעיים שאינם בבנק. זה מדד על **הבנק**, לא על הפריטים.

⚠ חתימה לפי מנה: `base4` 89% · `mid6` 85% · `aca6` 81% · `adv6/7` 79% ·
`mid5/adv8` 75% · `aca8` 70% · **`aca7` 59%**. `aca7` היא גם המנה שמיפתחה מחדש
9 פריטים במלחמת המפתחות. ממוצע כללי היה מחביא אותה.

## מה פתוח

1. ⚠ **רשימות המילים אינן מעודכנות.** `words/*.md` מכילות מילים שהפכו למפתחות
   בסבב ההרחבה. אינו פוגע ב-450 הקיימים, יטעה כותב בסבב הבא. הפתרון:
   `node sentence-completion/band_words.js`.
2. ⚠ **`p=none` בלי `rua=`** ב-DMARC של `800-plus.com`. אין מי שאוסף דוחות אימות,
   ולכן **אין מדידה של מסירת מיילים**. DKIM קיים, ה-SPF של Resend על
   `send.800-plus.com`, וההגדרה כנראה תקינה — אבל אין הוכחה. שינוי DNS בחשבון
   Cloudflare של חגי, ולא שלי.
3. ⚠ **`origin/pipeline-v2` מחזיק 419 קבצי `units_output/` בגיטהאב.** לא באתר
   (404 מאומת). נמחק פעם אחת לבקשת חגי והסשן המקביל דחף מחדש כנקודת שמירה.
   החלטת תיאום, לא פעולה טכנית.
4. ℹ פריטים בדרגת WEAK מהמדידה שלא תוקנו (27 ב-246 החדשים). לא כשלים.
5. ℹ ההתקדמות בתרגול מסתנכרנת בין מכשירים דרך בלוב ה-extras. אין טבלה נפרדת.

## ⛔ כללים עומדים

- **אין `git add -A`.** הריפו ציבורי, ודלפו ממנו מיילים ו-`user_id` בעבר. נתיבים
  מפורשים בלבד. אין `git stash`, `reset --hard`, `clean -fd`.
- **לא לפרסם מילים** עד שחגי יגדיר אחרת. משפטים מותר.
- **כל הכסף מוקפא** — תנאי שימוש, פרטיות, `billing-webhook`, `create-checkout`.
- **חשיפה משפטית** — ראה `CLAUDE.md` בשורש. לא להכריע לבד.
- ⚠ **שלוש תיקיות עבודה על אותו מאגר** (`800+`, `800plus-licensing`,
  `800plus-marketing`). העלאה נעשית מענף שנוצר מ-`origin/main`, לוקחת קבצים
  מפורשים, ומעלה REV על הגרסה שב-`main` — **לא** מהעץ המקומי, שנושא עבודה של
  סשנים אחרים. אחרי הדחיפה, cherry-pick של ה-REV חזרה לענף העבודה.

## הלקח שחוזר, ולכן הוא כלל

**שער שמדווח "עבר" בלי הוכחת שיניים אינו עדות.** קרה חמש פעמים בפרויקט הזה:
שער שבדק שאף מילה לא זזה בזמן שהסוגריים נחתו במקום הלא נכון · תנאי שקרס לשקר
תמיד ולכן דגל שמעולם לא נורה · שער שירה 184 דגלים שרובם שקריים · שער מבני שירה
78 "כשלים" שהיו התכנון התקין · ומדד שספר פסיקים במקום נקודה-פסיק והחזיר "נכשל"
על 99% הצלחה.
מכאן: שער חדש מגיע עם הרצה על מקרה שאמור להיפסל **ועל מקרה שאמור לעבור**, והמספר
הראשון שהוא מפיק נבדק לגופו לפני שמסתמכים עליו.
