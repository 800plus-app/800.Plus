# לפני גבייה — שבע בדיקות שחייבות לעבור
עודכן: 5.9.2026 · נבדק בפועל מול הפרויקט החי (curl קריאה בלבד).
⛔ אף אחת מהשבע אינה ירוקה במלואה היום. **אסור לגבות שקל לפני ששבע השורות ✓.**

| # | בדיקה | מצב היום | בעלים |
|---|---|---|---|
| א | delete-account מחזיר 401 בלי אימות | ✓ חלקית (ראו הערה) | מנהל |
| ב | billing-webhook /health מחזיר 200 | ✗ 404 — לא פרוסה | חגי (deploy) |
| ג | send-push פרוסה ועונה | ✗ 404 — לא פרוסה | חגי (deploy) |
| ד | מיגרציות 20–21 «לא נבדק» | ✗ לא אומת | חגי (SQL) |
| ה | מיגרציות 23 + 24 «לא רץ» | ✗ לא רצו | חגי (SQL) |
| ו | בידוד RLS על טבלאות החיוב | ✗ לא ניתן לבדוק עד תשלום ראשון | חגי (SQL) |
| ז | ADMIN_EMAILS מוגדר כסוד | ✗ לא הוכרע (החלטות 31.8) | חגי |

---

## א · delete-account דוחה בקשה בלי אימות

**פקודה:**
```
curl -s -o /dev/null -w "%{http_code}" -X POST https://oycypbnzcvtjliovfsxn.supabase.co/functions/v1/delete-account
```
**פלט צפוי:** `401`

**מצב היום (נמדד 5.9.2026):** `401` · גוף: `{"code":"UNAUTHORIZED_NO_AUTH_HEADER","message":"Missing authorization header"}` — הדחייה עובדת ✓.
⚠ אבל הגוף הוא של **השער של Supabase**, לא של הפונקציה — כלומר `verify_jwt` עדיין דלוק בייצור, בניגוד ל-`config.toml` (שורות 38-39: `[functions.delete-account] verify_jwt = false`). זה תואם למדידת 27.8 שמתועדת בקובץ. הסיכון: במעבר למפתחות אסימטריים השער יחסום גם משתמשים לגיטימיים. **אחרי ה-deploy הבא** להריץ שוב ולוודא שהגוף משתנה לקודי הפונקציה עצמה (NO_AUTH_HEADER וכד'). **בעלים: מנהל** (מעקב אחרי deploy).

## ב · billing-webhook חי — /health מחזיר 200

**פקודה:**
```
curl -i https://oycypbnzcvtjliovfsxn.supabase.co/functions/v1/billing-webhook/health
```
**פלט צפוי:** `200` עם גוף JSON. אם `401` — ‏`verify_jwt` לא נתפס (config.toml שורות 15-16); אם `404` — לא פרוסה.

**מצב היום (נמדד 5.9.2026):** `404` · `{"code":"NOT_FOUND","message":"Requested function was not found"}` — **הפונקציה לא פרוסה** ✗.
**מה חגי מריץ:** מהתיקייה הראשית של הפרויקט, בטרמינל רגיל:
```
supabase functions deploy billing-webhook
```
ואז את ה-curl למעלה. צריך לחזור 200. **בעלים: חגי** (הפריסה דורשת התחברות לחשבון).

## ג · send-push פרוסה

**פקודה:**
```
curl -s -o /dev/null -w "%{http_code}" -X POST https://oycypbnzcvtjliovfsxn.supabase.co/functions/v1/send-push
```
**פלט צפוי:** ‏`401` (נדחה בלי הסוד — זה טוב). ‏`404` פירושו לא פרוסה.

**מצב היום (נמדד 5.9.2026):** `404` — **לא פרוסה** ✗. בלעדיה אין התראות יומיות, וה-workflow המתוזמן נכשל בשקט.
**מה חגי מריץ:** `supabase functions deploy send-push`, ואז את ה-curl. צריך לחזור 401 (לא 404). **בעלים: חגי.**

## ד · מיגרציות 20–21 (הקשחת הרשאות) — לוודא שנתפסו

לפי `migrations/README.md` שורה 28: רצו אבל «❓ לא נבדק».

**פקודה (חגי, ב-SQL Editor בדשבורד):**
```sql
select grantee, table_name, privilege_type
from information_schema.role_table_grants
where grantee in ('anon','PUBLIC') and table_schema='public'
  and privilege_type in ('INSERT','UPDATE','DELETE');
```
**פלט צפוי:** **0 שורות** — לאף אורח אין הרשאת כתיבה ישירה לטבלאות.

**מצב היום:** ✗ לא אומת. **בעלים: חגי** (דורש גישה לדשבורד). אם חוזרות שורות — לצלם ולהעביר למנהל.

## ה · מיגרציות 23 + 24 — להריץ

- **23** («⏳ לא רץ»): מעלה את מתג ההרדמה לראש הפונקציה — **חובה לפני פריסת billing-webhook**, כלומר לפני סעיף ב.
- **24** («⏳ לא רץ», נוספה 4.9): עמודת `kind` ב-`wtp_survey`. לא חוסמת גבייה, אבל בלעדיה משפך הסקר לא מדיד — להריץ באותה ישיבה.

**מה חגי מריץ:** ב-SQL Editor, להדביק את `migrations/23.sql` ואז את `migrations/24.sql` (ב-24 לעצור בין חלק 2 ל-3 ולהסתכל בפלט, כמו שכתוב בקובץ).
**פקודת אימות ל-24:**
```sql
select column_name from information_schema.columns
where table_name='wtp_survey' and column_name='kind';
```
**פלט צפוי:** שורה אחת — `kind`.

**מצב היום:** ✗ שתיהן לא רצו. **בעלים: חגי.** אחרי הריצה — לעדכן את `migrations/README.md`.

## ו · בידוד RLS על טבלאות החיוב

לפי `supabase/README.md` (~שורות 108-118): ב-27.8 שלוש טבלאות החיוב (`billing_customers` · `billing_subscriptions` · `payments`) חזרו «0 שורות · 0 משתמשים» — **RLS שם לא נבדק, כי הטבלאות ריקות.** הרגע היחיד לבדוק באמת: **אחרי התשלום האמיתי הראשון, לפני שיש לקוחות רבים.**

**פקודה (חגי, אחרי התשלום הראשון):** להריץ את `supabase/rls-isolation-all-tables.sql` ב-SQL Editor.
**פלט צפוי:** לכל אחת משלוש טבלאות החיוב, בשני הכיוונים: `רואה_את_האחר = 0` **וגם** `רואה_את_עצמו > 0`.

**מצב היום:** ✗ לא ניתן לבדוק עדיין (אין שורות). זו הבדיקה היחידה שסוגרים **מיד אחרי** השקל הראשון, לא לפניו — ולכן היא נשארת ברשימה כתנאי. **בעלים: חגי.**

## ז · ADMIN_EMAILS מוגדר לפני הפריסה

לפי `החלטות-800plus.md` (31.8, «השומר על חשבון הניהול · לא הוכרע»): המייל יצא מהקוד למשתנה סביבה `ADMIN_EMAILS`. ⛔ **פריסת delete-account בלי להגדיר את הסוד קודם פותחת את חשבון הניהול למחיקה.**

**מה חגי עושה:** בדשבורד → Edge Functions → Secrets, לוודא שקיים סוד בשם `ADMIN_EMAILS` והערך הוא כתובת המייל של חשבון הניהול. או בטרמינל רגיל:
```
supabase secrets list
```
**פלט צפוי:** שורה עם `ADMIN_EMAILS` ברשימה.

**מצב היום:** ✗ ההכרעה עצמה (סוד או קשיח) עדיין פתוחה — לסגור אותה לפני כל deploy של delete-account. **בעלים: חגי.**

---

## סדר הפעולות המומלץ
1. חגי מכריע ומגדיר `ADMIN_EMAILS` (ז) → 2. מריץ מיגרציה 23 (ה) → 3. פורס billing-webhook + send-push ומאמת ב-curl (ב, ג) → 4. מאמת 20-21 ומריץ 24 (ד, ה) → 5. אחרי deploy: מאמת שוב את א → 6. תשלום בדיקה ראשון → מיד rls-isolation-all-tables (ו).
